import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'encheremobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
